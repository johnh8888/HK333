name: 老澳门六合彩自动预测

on:
  schedule:
    # 根据实际开奖时间调整：北京时间 23:00 (UTC 15:00)
    - cron: '0 15 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  predict:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: |
          pip install --upgrade pip
          pip install numpy scikit-learn lightgbm xgboost catboost

      - name: 运行 old_macau.py 同步、回测、调优与预测
        run: python old_macau.py sync --with-backtest --auto-tune

      - name: 运行 macau.py 同步、回测、调优与预测
        run: python macau.py sync --with-backtest --auto-tune

      - name: 提交数据库更新
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add old_macau.db macau.db
          git commit -m "🤖 老澳门自动更新 $(TZ='Asia/Hong_Kong' date +'%Y-%m-%d %H:%M')" || echo "Nothing to commit"
          git push

      - name: 保存 old_macau.db 备份
        uses: actions/upload-artifact@v4
        with:
          name: old-macau-db-backup
          path: old_macau.db

      - name: 保存 macau.db 备份
        uses: actions/upload-artifact@v4
        with:
          name: macau-db-backup
          path: macau.db
